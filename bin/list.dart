void main(List<String> args) {
  List<String> name = ['Minh', 'Duc', 'Duong'];
  name.forEach((element) {
      print(element);
    }); 

  //name.add('Nguyen');
  // name.addAll(['Khang', 'Nhat', 'Luan']);
  // print(name);

  // name.insert(name.length, "Cao");
  // name.insertAll(1, ["Cao", 'Nguyen', 'Nhat']);
  // print(name);

  // name.remove('Cao');
  // name.removeAt(1);
  // name.removeLast();
  // name.removeRange(1, 2);
  // name.clear();
  //  print(name);
  
  // name[1] = 'Nguyen';
  // name.replaceRange(1, 2, ['Linh', 'Nhat']);
  // print(name);
}
